#!/bin/sh

if [[ "$SHELL" != $(which zsh) ]]; then
  echo "changing ${SHELL}"
  #chsh -s $(which zsh)
fi

brew tap thoughtbot/formulae && brew install rcm

brew install \
  elixir \
  gh \
  golang \
  gnupg \
  gpg-agent \
  gpg \
  nmap \
  pinentry-mac \
  sl \
  socat \
  tmux \
  tree
