#!/bin/sh

if [[ "$SHELL" != $(which zsh) ]]; then
  echo "changing ${SHELL}"
  #chsh -s $(which zsh)
fi

brew tap thoughtbot/formulae && brew install rcm

brew install elixir
brew install gh
brew install golang
brew install gpg
brew install nmap
brew install sl
brew install tmux
brew install tree
